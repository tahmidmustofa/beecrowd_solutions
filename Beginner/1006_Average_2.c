#include <stdio.h>

int main() {
    
    float A, B, C;
    scanf("%f %f %f", &A, &B, &C);
    
    float average = ((A * 2) + (B * 3) + (C * 5)) / (2 + 3 + 5);
    printf("MEDIA = %.1f\n", average);
    
    return 0;
    
}

// Solved by Tahmid Mustofa
